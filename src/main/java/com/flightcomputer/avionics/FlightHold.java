package com.flightcomputer.avionics;

/** Independent hold functions; they may be combined with a primary flight mode. */
public enum FlightHold {
    ALTITUDE,
    HEADING,
    POSITION,
    VELOCITY
}
