name: Phase 5 Validation

on:
  push:
    branches: [phase-5-validation]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
      - name: Build
        shell: bash
        run: |
          set +e
          bash ./gradlew clean build --no-daemon --stacktrace 2>&1 | tee build.log
          status=${PIPESTATUS[0]}
          {
            echo '## Gradle build output (last 250 lines)'
            echo '```text'
            tail -n 250 build.log
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
          exit "$status"
